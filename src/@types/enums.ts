export enum MilestoneStatus {
  PENDING = "PENDING",
  SUBMITTED = "SUBMITTED",
  APPROVED = "APPROVED",
  REJECTED = "REJECTED",
}

export enum LogType {
  ACTIVITY = "ACTIVITY",
  ERROR = "ERROR",
  SECURITY = "SECURITY",
  TRANSACTION = "TRANSACTION",
}

export enum LogStatus {
  INFO = "INFO",
  WARNING = "WARNING",
  ERROR = "ERROR",
  CRITICAL = "CRITICAL",
}
