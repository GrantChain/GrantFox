import { PayoutProviderProfile } from "@/app/(auth)/payout-provider";

export default function PayoutProviderProfilePage() {
  return (
    <div className="container max-w-4xl mx-auto px-4 sm:px-6 lg:px-8 py-6">
      <PayoutProviderProfile />
    </div>
  );
}
